print('*** 문자열 피라미드 ***')

#word에 10자 이내의 문자열을 받는다
word = str(input('문자열(10자 이내)? '))

#word에 최소 1자리 문자열이 들어오기 때문에
#10을 곱하여 10자리 문자열이 될 수 있는
#변수 result를 만들어 저장한다.
result = word*10

#빈칸을 4개 출력하고 첫번째 문자열만 나오게 출력한다. 
print(' '*4 + result[:1])

#빈칸을 3개 출력하고 세번째 문자열까지 나오게 출력한다. 
print(' '*3 + result[0:3])

#빈칸을 2개 출력하고 다섯번째 문자열까지 나오게 출력한다. 
print(' '*2 + result[0:5])

#빈칸을 1개 출력하고 일곱번째 문자열까지 나오게 출력한다. 
print(' ' + result[0:7])

#빈칸 없이 처음부터 아홉번째 문자열까지 출력한다.
print(result[0:9])
